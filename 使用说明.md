# WeixinClawBot 一键连接使用说明

## 适合谁

本工具适合已经在 Windows 上安装 Hermes Agent，并希望通过手机微信长期使用 Hermes 的用户。

## 一、第一次使用前的准备

### 1. 安装 Hermes Agent

请使用 Hermes 官方文档：

https://hermes-agent.nousresearch.com/docs/

安装完成后，在 Windows 终端执行：

```text
hermes doctor
```

### 2. 配置微信

在终端执行：

```text
hermes gateway setup
```

选择 **Weixin**，按照提示扫码绑定。

> 不要把微信 Token、账号 ID、`.env` 内容或二维码截图上传到 GitHub。

## 二、下载发布包

进入：

https://github.com/JeskoLu/WeixinClawBot-Launcher/releases/latest

初学者建议下载：

```text
WeixinClawBot-Launcher-v1.0.0.zip
```

解压后会看到：

```text
WeixinClawBot-OneClick.exe
Hermes_Gateway_Watchdog.vbs
Hermes_Gateway_Startup.vbs
使用说明.md
LICENSE
```

## 三、安装守护脚本

按 `Win + R`，输入：

```text
%LOCALAPPDATA%\hermes\gateway-service
```

按 Enter。如果目录不存在，请先确认 Hermes Agent 已安装。

将下面两个文件复制进去：

```text
Hermes_Gateway_Watchdog.vbs
Hermes_Gateway_Startup.vbs
```

如果你自定义了 `HERMES_HOME`，请复制到 `%HERMES_HOME%\gateway-service`。

## 四、连接微信机器人

双击：

```text
WeixinClawBot-OneClick.exe
```

成功时会弹出连接成功提示。之后可直接在手机微信中给机器人发送消息。

## 五、确认网关状态

在终端运行：

```text
hermes gateway status
```

如需前台查看完整日志：

```text
hermes gateway run
```

停止网关：

```text
hermes gateway stop
```

## 六、开机自动连接

如果 Hermes 已使用官方方式安装为登录自启，可运行：

```text
hermes gateway install
```

也可以将 `Hermes_Gateway_Startup.vbs` 注册到当前用户的 Windows 登录启动项。初学者不建议随意修改注册表；优先使用 Hermes 官方 `gateway install` 命令。

## 七、常见故障

### 1. 未找到 Hermes 目录

检查：

```text
%LOCALAPPDATA%\hermes
```

如果 Hermes 安装在其他位置，请设置 Windows 用户环境变量：

```text
HERMES_HOME=你的Hermes目录
```

### 2. 微信仍收不到消息

依次执行：

```text
hermes doctor
hermes gateway status
hermes gateway run
```

查看是否出现 `weixin connected`。

### 3. 双击 EXE 没反应

- 检查 Windows 安全中心是否拦截；
- 右键文件 → 属性，确认是否有“解除锁定”；
- 校验 SHA-256；
- 从源码运行 `python src\weixin_launcher.py` 查看问题。

### 4. 安全软件误报

PyInstaller 单文件程序没有商业数字签名时可能被少数安全软件误报。请从本仓库官方 Release 下载并核对校验值；不放心时可自行审查源码和打包。

## 八、安全规则

禁止公开或提交：

```text
.env
config.yaml 中的敏感字段
微信 Token
账号 ID
API Key
Hermes 会话与日志
```

本工具不需要你向项目作者提供上述信息。
